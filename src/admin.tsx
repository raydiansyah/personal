/**
 * Module: Owner admin application
 * Purpose: Authenticate owner and manage portfolio, testimonials, slides, and contact inbox
 * Used by: TanStack routes /auth/login and /admin
 * Dependencies: React, Supabase owner data service, Cloudflare Turnstile widget, app.css
 * Public functions: AdminApp()
 * Side effects: Auth session, role-gated database CRUD, and R2 presigned uploads through RLS
 */
import { useEffect, useState, type FormEvent } from 'react';
import { useLocation, useNavigate } from '@tanstack/react-router';
import { createPortfolio, createTestimonial, deletePortfolio, getOwnerSession, listContactMessages, listOwnerPortfolio, listSlides, listTestimonials, signInOwner, signOutOwner, slugify, uploadPortfolioCover, uploadSlide, type Slide, type Testimonial, type UploadProgress } from './lib/admin';
import type { Portfolio } from './lib/portfolio';
import { APP_VERSION } from './lib/version';
import { TurnstileWidget } from './components/turnstile-widget';
import { PortfolioPreview } from './components/portfolio-preview';
import { getPortfolioPreviewUrl } from './lib/portfolio-preview';
import { SlidePreview } from './components/slide-preview';
import './app.css';
import './admin-upload.css';

type Message = { id: string; nama: string; email: string; status: string };
export function AdminApp() { const [session, setSession] = useState(false); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [captchaToken, setCaptchaToken] = useState(''); const [captchaKey, setCaptchaKey] = useState(0); const [items, setItems] = useState<Portfolio[]>([]); const [testimonials, setTestimonials] = useState<Testimonial[]>([]); const [slides, setSlides] = useState<Slide[]>([]); const [messages, setMessages] = useState<Message[]>([]); const [status, setStatus] = useState(''); const [slideTitle, setSlideTitle] = useState(''); const [projectTitle, setProjectTitle] = useState(''); const [projectUrl, setProjectUrl] = useState(''); const [portfolioQuery, setPortfolioQuery] = useState(''); const [portfolioCategory, setPortfolioCategory] = useState(''); const [slideQuery, setSlideQuery] = useState(''); const [slideMime, setSlideMime] = useState(''); const [projectCover, setProjectCover] = useState<File | null>(null); const [uploading, setUploading] = useState(false); const [uploadProgress, setUploadProgress] = useState<UploadProgress | null>(null); const navigate = useNavigate(); const location = useLocation();
  async function load() { try { const current = await getOwnerSession(); const role = current.data.session?.user.app_metadata?.role; const isAdmin = role === 'admin' || role === 'owner'; if (current.data.session && !isAdmin) { await signOutOwner(); setSession(false); setStatus('Akun ini tidak memiliki akses admin.'); return; } setSession(Boolean(current.data.session)); if (current.data.session) { if (location.pathname === '/auth/login') await navigate({ to: '/admin', replace: true }); const [portfolio, social, presentation, inbox] = await Promise.all([listOwnerPortfolio(), listTestimonials(), listSlides(), listContactMessages()]); setItems(portfolio); setTestimonials(social); setSlides(presentation); setMessages(inbox as Message[]); } } catch (error) { setStatus(error instanceof Error ? error.message : 'Supabase environment belum dikonfigurasi.'); } }
  useEffect(() => { void load(); }, [location.pathname]);
  async function login(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setStatus(''); if (!captchaToken) { setStatus('Selesaikan CAPTCHA terlebih dahulu.'); return; } try { const result = await signInOwner(email, password, captchaToken); if (result.error) throw result.error; const role = result.data.user?.app_metadata?.role; if (role !== 'admin' && role !== 'owner') { await signOutOwner(); throw new Error('Akun ini tidak memiliki akses admin.'); } await navigate({ to: '/admin', replace: true }); } catch (error) { setStatus(error instanceof Error ? error.message : 'Login gagal.'); } finally { setCaptchaToken(''); setCaptchaKey((value) => value + 1); } }
  async function addProject(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const target = event.currentTarget; const form = new FormData(target); try { const slug = slugify(String(form.get('slug'))); const demoUrl = String(form.get('url_demo') ?? '').trim() || null; const coverUrl = projectCover ? await uploadPortfolioCover(projectCover, slug) : String(form.get('url_gambar') ?? '').trim() || getPortfolioPreviewUrl(demoUrl) || null; await createPortfolio({ judul: String(form.get('judul')), slug, kategori: String(form.get('kategori')) as Portfolio['kategori'], ringkasan: String(form.get('ringkasan')), url_demo: demoUrl, url_gambar: coverUrl }); target.reset(); setProjectTitle(''); setProjectUrl(''); setProjectCover(null); await load(); setStatus('Portfolio tersimpan.'); } catch (error) { setStatus(error instanceof Error ? error.message : 'Gagal menyimpan portfolio.'); } }
  async function removeProject(id: string) { if (!window.confirm('Sembunyikan portfolio ini dari publik?')) return; try { await deletePortfolio(id); await load(); setStatus('Portfolio disembunyikan.'); } catch (error) { setStatus(error instanceof Error ? error.message : 'Gagal mengubah portfolio.'); } }
  async function addTestimonial(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const target = event.currentTarget; const form = new FormData(target); try { await createTestimonial({ nama: String(form.get('nama')), jabatan: String(form.get('jabatan')), kutipan: String(form.get('kutipan')) }); target.reset(); await load(); setStatus('Testimonial tersimpan.'); } catch (error) { setStatus(error instanceof Error ? error.message : 'Gagal menyimpan testimonial.'); } }
  async function addSlide(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (uploading) return; const target = event.currentTarget; const form = new FormData(target); const file = form.get('file'); const title = String(form.get('judul') ?? '').trim(); if (!(file instanceof File) || !file.size) return setStatus('Pilih file HTML atau PDF.'); if (file.size > 10 * 1024 * 1024) return setStatus('Ukuran file maksimal 10MB.'); setStatus('Menyiapkan upload…'); setUploading(true); setUploadProgress({ phase: 'preparing', percent: 0 }); try { await uploadSlide(file, title, slugify(title), setUploadProgress); target.reset(); setSlideTitle(''); await load(); setStatus('Slide tersimpan.'); } catch (error) { setStatus(error instanceof Error ? error.message : 'Gagal mengupload slide.'); } finally { setUploading(false); setUploadProgress(null); } }
  const filteredPortfolio = items.filter((item) => (!portfolioCategory || item.kategori === portfolioCategory) && (!portfolioQuery || `${item.judul} ${item.slug} ${item.kategori}`.toLowerCase().includes(portfolioQuery.toLowerCase())));
  const filteredSlides = slides.filter((item) => (!slideMime || item.mime_type === slideMime) && (!slideQuery || `${item.judul} ${item.slug}`.toLowerCase().includes(slideQuery.toLowerCase())));
  if (!session) return <main className="page section"><p className="eyebrow">Owner access</p><h1>Admin studio.</h1><form className="form" onSubmit={login}><input type="email" placeholder="Email owner" value={email} onChange={(event) => setEmail(event.target.value)} required /><input type="password" placeholder="Password" value={password} onChange={(event) => setPassword(event.target.value)} required /><TurnstileWidget key={captchaKey} onToken={setCaptchaToken} onError={setStatus} /><button className="button primary" type="submit">Sign in ↗</button>{status && <p className="status" role="alert">{status}</p>}<small className="muted">Version {APP_VERSION}</small></form></main>;
  return <main className="page section"><div className="section-heading"><div><p className="eyebrow">Content studio</p><h1>Owner dashboard.</h1></div><button className="button" onClick={() => void signOutOwner().then(() => setSession(false))}>Log out</button></div>{status && <p className="status" role="status">{status}</p>}<section className="grid"><form className="card form" onSubmit={addProject}><span className="tag">New portfolio</span><input name="judul" placeholder="Judul project" value={projectTitle} onChange={(event) => setProjectTitle(event.target.value)} required /><input name="slug" placeholder="slug otomatis" value={slugify(projectTitle || projectUrl)} readOnly aria-label="Slug portfolio (otomatis)" /><select name="kategori" defaultValue="website"><option value="website">Website</option><option value="aplikasi-web">Aplikasi web</option><option value="company-profile">Company profile</option></select><textarea name="ringkasan" placeholder="Ringkasan" required /><input name="url_demo" type="url" placeholder="https://project-live.com" value={projectUrl} onChange={(event) => setProjectUrl(event.target.value)} /><input name="url_gambar" type="url" placeholder="URL cover manual (opsional)" /><input name="cover" type="file" accept="image/*" onChange={(event) => setProjectCover(event.target.files?.[0] ?? null)} /><small className="muted">Screenshot otomatis diambil dari link demo. Jika gagal, gunakan cover manual atau upload gambar opsional (maks. 5MB).</small>{projectUrl && <PortfolioPreview title={projectTitle || 'Preview project'} demoUrl={projectUrl} compact />}{projectCover && <small className="muted">Cover dipilih: {projectCover.name}</small>}<button className="button primary">Simpan portfolio ↗</button></form><article className="card admin-table-card"><div className="section-heading"><div><span className="tag">Portfolio · {filteredPortfolio.length}/{items.length}</span><h3>Portfolio list</h3></div><div className="admin-filters"><input aria-label="Cari portfolio" placeholder="Cari judul atau slug" value={portfolioQuery} onChange={(event) => setPortfolioQuery(event.target.value)} /><select aria-label="Filter kategori" value={portfolioCategory} onChange={(event) => setPortfolioCategory(event.target.value)}><option value="">Semua kategori</option><option value="website">Website</option><option value="aplikasi-web">Aplikasi web</option><option value="company-profile">Company profile</option></select></div></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Preview</th><th>Judul</th><th>Kategori</th><th>Slug</th><th>Aksi</th></tr></thead><tbody>{filteredPortfolio.map((item) => <tr key={item.id}><td><PortfolioPreview title={item.judul} demoUrl={item.url_demo} coverUrl={item.url_gambar} compact /></td><td><strong>{item.judul}</strong></td><td>{item.kategori}</td><td>{item.slug}</td><td><button className="tag" onClick={() => void removeProject(item.id)}>Hide</button></td></tr>)}</tbody></table>{filteredPortfolio.length === 0 && <p className="muted">Tidak ada portfolio yang cocok.</p>}</div></article><form className="card form" onSubmit={addTestimonial}><span className="tag">New testimonial</span><input name="nama" placeholder="Nama" required /><input name="jabatan" placeholder="Jabatan" /><textarea name="kutipan" placeholder="Kutipan" required /><button className="button primary">Simpan testimonial ↗</button></form><article className="card"><span className="tag">Testimonials · {testimonials.length}</span>{testimonials.map((item) => <p key={item.id}><strong>{item.nama}</strong><br /><small className="muted">{item.kutipan}</small></p>)}</article><form className="card form" onSubmit={addSlide}><span className="tag">Upload slide</span><input name="judul" placeholder="Judul slide" value={slideTitle} onChange={(event) => setSlideTitle(event.target.value)} required /><input name="slug" placeholder="slug otomatis" value={slugify(slideTitle)} readOnly aria-label="Slug slide (otomatis)" /><input name="file" type="file" accept=".html,.pdf,text/html,application/pdf" required /><small className="muted">HTML/PDF, maksimal 10MB. Slug dibuat otomatis dari judul.</small>{uploadProgress && <div className="upload-progress" role="status"><progress max="100" value={uploadProgress.percent} /> <span>{uploadProgress.phase === 'preparing' ? 'Menyiapkan…' : uploadProgress.phase === 'saving' ? 'Menyimpan metadata…' : `Mengupload… ${uploadProgress.percent}%`}</span></div>}<button className="button primary" type="submit" disabled={uploading}>{uploading ? 'Upload berjalan…' : 'Upload slide ↗'}</button></form><article className="card admin-table-card"><div className="section-heading"><div><span className="tag">Slides · {filteredSlides.length}/{slides.length}</span><h3>Slide list</h3></div><div className="admin-filters"><input aria-label="Cari slide" placeholder="Cari judul atau slug" value={slideQuery} onChange={(event) => setSlideQuery(event.target.value)} /><select aria-label="Filter tipe slide" value={slideMime} onChange={(event) => setSlideMime(event.target.value)}><option value="">Semua tipe</option><option value="text/html">HTML</option><option value="application/pdf">PDF</option></select></div></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Preview</th><th>Judul</th><th>Slug</th><th>Tipe</th><th>Link</th></tr></thead><tbody>{filteredSlides.map((item) => <tr key={item.id}><td><SlidePreview title={item.judul} storagePath={item.storage_path} mimeType={item.mime_type} /></td><td><strong>{item.judul}</strong></td><td>{item.slug}</td><td>{item.mime_type === "text/html" ? "HTML" : "PDF"}</td><td><a className="tag" href={"/s/" + item.slug} target="_blank" rel="noreferrer">Open ↗</a></td></tr>)}</tbody></table>{filteredSlides.length === 0 && <p className="muted">Tidak ada slide yang cocok.</p>}</div></article><article className="card"><span className="tag">Inbox · {messages.length}</span>{messages.slice(0, 6).map((item) => <p key={item.id}><strong>{item.nama}</strong><br /><small className="muted">{item.email} · {item.status}</small></p>)}</article></section></main>; }
